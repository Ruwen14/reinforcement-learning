name: rl-stack-pytorch
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  - pip
  - pip:
    - gym
    - numpy
    - matplotlib
    - wandb
    - pandas

  # run conda install pytorch torchvision torchaudio pytorch-cuda=11.7 -c pytorch -c nvidia